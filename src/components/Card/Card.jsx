import React from "react";
import "";

const Card = ({ item }) => {
  return;

  <div className="row">
    <div className="img">{item.img}</div>
    <div className="haeding">{item.name}</div>
    <div className="comment">{item.comment}</div>
  </div>;
};

export default Card;
